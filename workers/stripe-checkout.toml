name = "melhorzona-stripe-checkout"
main = "stripe-checkout.js"
compatibility_date = "2025-05-31"

# Substituir pelos price_ID reais criados no Stripe Dashboard
# Ver instruções em CLAUDE.md → secção Stripe
[vars]
STRIPE_PRICE_ID_UNITARIO = "STRIPE_PRICE_ID_UNITARIO"
STRIPE_PRICE_ID_MENSAL   = "STRIPE_PRICE_ID_MENSAL"

# Secrets (nunca em texto claro — usar wrangler secret put):
# cd workers && printf 'sk_live_...' | wrangler secret put STRIPE_SECRET_KEY --config stripe-checkout.toml
